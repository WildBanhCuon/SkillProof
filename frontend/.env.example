# Local dev (npm run dev proxies /v1 to localhost:3000 if unset)
VITE_API_URL=http://localhost:3000/v1

# Production (Render static site build env — no trailing slash):
# VITE_API_URL=https://skillproof-api-mxjo.onrender.com/v1
